apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-explorer-aggregator.fullname" . }}
  labels:
    {{- include "cf-explorer-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-explorer-aggregator.selectorLabels" . | nindent 4 }}
