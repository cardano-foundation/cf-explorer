{{ if index .Values "cf-ledger-sync" "enabled" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cf-ledger-sync
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/cardano-foundation/cf-ledger-sync.git
    targetRevision: {{ .Values.cf-ledger-sync.git.targetRevision | default "HEAD" }}
    path: deploy/cf-ledger-sync
    helm:
      parameters:
        - name: "domain"
          value: {{ .Values.domain }}
        - name: "network"
          value: {{ .Values.network }}
      valueFiles:
      - values-{{ .Values.network }}.yaml
      {{- with index .Values "cf-ledger-sync" "values" }}
      values: |
{{ toYaml . | indent 8 }}
      {{- end }}

  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true

  destination:
    namespace: cf-explorer
    server: {{ .Values.spec.destination.server }}
{{ end }}
