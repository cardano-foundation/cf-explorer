{{ with .Values.explorerRewardsApi }}
{{ if .enabled }}
  {{- range $v := $.Values.ingressDomains }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "cf-explorer-rewards-api-{{ $v.name }}"
spec:
  rules:
    {{- if eq $.Values.network "mainnet" }}
    - host: "rewards-api.{{ $v.domain }}"
    {{- else }}
    - host: "rewards-api.{{ $.Values.network }}.{{ $v.domain }}"
    {{- end }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cf-explorer-rewards-api
                port:
                  number: {{ .port }}
  tls:
    - hosts:
        {{- if eq $.Values.network "mainnet" }}
        - "rewards-api.{{ $v.domain }}"
        {{- else }}
        - "rewards-api.{{ $.Values.network }}.{{ $v.domain }}"
        {{- end }}
  {{- end }}
{{ end }}
{{ end }}
