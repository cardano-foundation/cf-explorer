{{ with .Values.explorerRewardsApi }}
{{ if .enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cf-explorer-rewards-api
spec:
  rules:
    {{- if eq $.Values.network "mainnet" }}
    - host: "rewards-api.{{ $.Values.domain }}"
    {{- else }}
    - host: "rewards-api.{{ $.Values.network }}.{{ $.Values.domain }}"
    {{- end }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cf-explorer-rewards-api
                port:
                  number: {{ .port }}
  tls:
    - hosts:
        {{- if eq $.Values.network "mainnet" }}
        - "rewards-api.{{ $.Values.domain }}"
        {{- else }}
        - "rewards-api.{{ $.Values.network }}.{{ $.Values.domain }}"
        {{- end }}
{{ end }}
{{ end }}
