apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-network-monitoring-aggregator.fullname" . }}
  labels:
    {{- include "cf-network-monitoring-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-network-monitoring-aggregator.selectorLabels" . | nindent 4 }}
