git:
  targetRevision: "feat/MET-755-Create_explorer_helm_chart"

stage: dev

network: preprod

traefik:
  enabled: false

argo-cd:
  server:
    ingress:
      hosts:
        - argocd.{{ tpl .Value.domain . }}
